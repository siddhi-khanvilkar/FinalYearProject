<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Messages for {{parentName}}</title>
  <link rel="stylesheet" href="/style.css"> <!-- if you have a CSS file -->
</head>
<body>
  <h1>Messages for {{parentName}}</h1>

  {{#if messages.length}}
    <ul>
      {{#each messages}}
        <li>
          <strong>From Teacher:</strong> {{student_name}} <br>
          <em>{{message}}</em> <br>
          <small>Message ID: {{id}}</small>
        </li>
        <hr>
      {{/each}}
    </ul>
  {{else}}
    <p>No messages yet.</p>
  {{/if}}

  <a href="/profile">Back to Dashboard</a>
</body>
</html>
